version: '3.8'

services:
  telegram-bot:
    build: .
    image: telegram-bot
    container_name: telegram-bot-base
    restart: unless-stopped
    environment:
    - BOT_TOKEN=${BOT_TOKEN}
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
